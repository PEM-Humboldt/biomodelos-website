$.ajax({
    type: "POST",
    url: "/tasks/tasks_by_group",
    data: { id: <%= params[:task][:group_id] %>},
    success:function(data) {
    	<% if @tasks.size > 1 %>
    		alertify.alert("<%= t('biomodelos.tasks.tasks_added_notice') %>");
    	<% else %>
    		alertify.alert("<%= t('biomodelos.tasks.task_added_notice') %>");
    	<% end %>
    }
});

