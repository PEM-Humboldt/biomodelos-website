$.ajax({
    type: "POST",
    url: "/tasks/tasks_by_group",
    data: { id: <%= @task.group_id %>},
    success:function(data) {
    	alertify.alert("<%= t('biomodelos.tasks.task_deleted_notice') %>");
    }
});
