$.ajax({
    type: "POST",
    url: "/tasks/tasks_by_group",
    data: { id: <%= @task.group_id %>},
    success:function(data) {
    	alertify.alert("La tarea ha sido eliminada con éxito");
    }
});
